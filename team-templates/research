#!/bin/bash
# research - Spawn a team for the research workflow
# Roles: conductor, researcher
# Usage: research [backend] [workdir]

set -e

BACKEND="${1:-claude}"
WORKDIR="${2:-$(pwd)}"

anvilspawn "$BACKEND" conductor  "$WORKDIR"
anvilspawn "$BACKEND" researcher "$WORKDIR"
