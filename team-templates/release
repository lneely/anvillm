#!/bin/bash
# release - Spawn a team for the release workflow
# Roles: conductor, devops
# Usage: release [backend] [workdir]

set -e

BACKEND="${1:-claude}"
WORKDIR="${2:-$(pwd)}"

anvilspawn "$BACKEND" conductor "$WORKDIR"
anvilspawn "$BACKEND" devops    "$WORKDIR"
