#!/bin/bash
# spike - Spawn a team for the spike workflow
# Roles: conductor, researcher
# Usage: spike [backend] [workdir]

set -e

BACKEND="${1:-claude}"
WORKDIR="${2:-$(pwd)}"

anvilspawn "$BACKEND" conductor  "$WORKDIR"
anvilspawn "$BACKEND" researcher "$WORKDIR"
