# Claude backend requirements
filesystem:
  rw:
    - "{HOME}/.claude"
    - "{HOME}/.claude.json"
    - "{HOME}/.config/anvillm"
  rwx:
    - "{HOME}/.claude/hooks"
network:
  unrestricted: true
env:
  - ANTHROPIC_API_KEY
  - CLAUDE_AGENT_NAME=anvillm-agent
