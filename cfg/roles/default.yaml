# Default role: permissive settings for ad-hoc usage
# Equivalent to current global sandbox.yaml behavior
filesystem:
  rwx:
    - "{CWD}"
    - "{PLAN9}/bin"
  rox:
    - "{HOME}/bin"
    - "{HOME}/.local/bin"
network:
  unrestricted: true
