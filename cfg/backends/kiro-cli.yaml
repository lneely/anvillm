# Kiro backend requirements
filesystem:
  rox:
    - "{HOME}/.local/bin"
  rw:
    - "{HOME}/.kiro"
    - "{HOME}/.aws"
    - "{XDG_DATA_HOME}/kiro-cli"
    - "{CWD}/.kiro"
network:
  unrestricted: true
env:
  - KIRO_API_KEY
